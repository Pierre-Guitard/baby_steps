<%= simple_form_for(memory) do |f| %>
  <%= form.label :title %>
  <%= f.input :title %>

  <%= form.label :location %>
  <%= f.input :location %>

  <%= form.label :date %>
  <%= f.input :date %>

  <%= form.label :content %>
  <%= form.rich_text_area :content %>
  <%= f.submit %>
<% end %>
