<%= form_with model: memory do |f| %>
  <%= f.input :title %>

 <%= f.input :location %>

  <%= f.input :date, value: Date.today.to_s %>

  <%= f.rich_text_area :content %>
  
  <%= f.submit %>
<% end %>
